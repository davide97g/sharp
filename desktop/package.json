{
  "name": "sharp-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tauri 2 desktop shell wrapping the sharp web app (macOS, Windows, Linux).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
