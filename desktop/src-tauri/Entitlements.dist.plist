<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <key>com.apple.security.device.camera</key>
  <true/>
  <!-- JavaScriptCore under the hardened runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Deliberately WITHOUT com.apple.developer.aps-environment: that is a
       restricted entitlement, and a Developer-ID build claiming it without an
       embedded provisioning profile is killed at launch by AMFI. Native APNs
       therefore stays off in this bundle; push falls back to the WebSocket,
       local notifications and web push (see the notifications section of the
       root CLAUDE.md). To turn APNs on, add the Push Notifications capability
       to the dev.sharp.app App ID, create a Developer ID provisioning profile
       for it, embed it as Contents/embedded.provisionprofile, and sign with
       Entitlements.plist instead. -->
</dict>
</plist>
